{
  "name": "@dshm/shell",
  "version": "0.0.0",
  "private": true,
  "description": "Electron shell hosting the DSH Entity Manager UI and per-entity webviews",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsc -p tsconfig.json && electron .",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "electron": "^37.2.0",
    "typescript": "^6.0.3"
  }
}
